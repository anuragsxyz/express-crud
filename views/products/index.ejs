<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Products Page</h1>
    <ul>
      <% for(let product of products){%>
      <li>
        <%= product.name %> ----- <%= product.category %> ----
        <u>INR<%= product.price %>/-</u>
        <a href="products/<%= product.id %>">Details</a>
      </li>

      <% } %>
    </ul>
  </body>
</html>
